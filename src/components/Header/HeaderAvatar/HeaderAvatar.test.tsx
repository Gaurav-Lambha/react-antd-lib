import React from "react";

import { render } from "@testing-library/react";

import HeaderAvatar from "./HeaderAvatar";

describe("HeaderAvatar", () => {
  test("render the HeaderAvatar component", () => {
    render(<HeaderAvatar />);
  });
});
